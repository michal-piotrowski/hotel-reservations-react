import { createBrowserHistory } from "history";
export const names = {
  ROOT: 'root',
  RESULTS_ID: 'results_id',
  RESULTS:'results',
  LOGIN:'login',
  PROFILE:'profile',
  TOS:'tos',
}

export default createBrowserHistory();